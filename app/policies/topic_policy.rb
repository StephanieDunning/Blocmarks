class TopicPolicy < ApplicationPolicy
  # def create?
  #   user.present?
  # end
  #
  # def update?
  #   raise record.topic.user.inspect
  #   user.present? && (record.topic.user == user)
  # end
  #
  # def destroy?
  #    user.present? && (record.topic.user == user)
  # end
end
